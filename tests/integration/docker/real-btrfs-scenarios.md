# Real-Btrfs scenario manifest

This manifest tracks the staged migration from `real-btrfs-test.sh`. The shell
scenario remains authoritative until every row has a C++ owner and parity has
been verified in the privileged container.

| Scenario | Setup | Public invocation | Exit | Expected artifacts and Btrfs state | Cleanup | Kernel capabilities | Owner |
|---|---|---|---|---|---|---|---|
| Full backup | Separate owned loop devices; Btrfs source; LUKS2/Btrfs target; two source files | `btrfs-backupctl runner execute` | 0 | JSON says completed/non-incremental; one read-only local snapshot; one committed received snapshot; matching content and UUID lineage; empty `.incoming` | Unmount both filesystems, close owned mapper, detach owned loops, remove marked root | loop, device-mapper, mount, Btrfs snapshot/send/receive | `RealBtrfsBackupTests.cpp` |
| Incremental backup | Completed full backup, then mutate and add source files | `btrfs-backupctl runner execute` with a new timestamp and run id | 0 | JSON says completed/incremental; two matching local and remote snapshots; latest received UUID points to latest local UUID | Same owned-resource teardown after the scenario sequence | loop, device-mapper, mount, Btrfs incremental send/receive | `RealBtrfsBackupTests.cpp` |
| Interrupted receive | Completed full and incremental backups; create an actual partial Btrfs subvolume below the source-specific incoming run directory | `btrfs-backupctl runner execute` with a third timestamp and run id | 0 | JSON says completed/incremental; stale incoming subvolume and its run directory are removed; no `.incoming/home` children remain; committed repository remains valid | Same owned-resource teardown; fixture cleanup refuses foreign loops or an unmarked root | loop, device-mapper, mount, Btrfs subvolume cleanup/send/receive | `RealBtrfsBackupTests.cpp` |
| Retention | Three successful generations, local and remote retention both set to two | Third public `btrfs-backupctl runner execute` | 0 | Exactly the latest two local and remote snapshots remain; latest contents and received UUID lineage match | Same owned-resource teardown after retention assertions | loop, device-mapper, mount, Btrfs snapshot deletion | `RealBtrfsBackupTests.cpp` |
| Pending recovery before receive | Pending marker and orphaned local snapshot | Public runner execute | 0 | Marker and orphan removed; later backup committed with valid lineage | Remove only fixture-owned pending and snapshot artifacts, then common teardown | loop, mount, Btrfs snapshots | `RealBtrfsBackupTests.cpp` |
| Pending recovery after commit | Pending marker references an already committed pair | Public runner execute | 0 | Marker removed; committed local and remote snapshots preserved | Remove fixture state, then common teardown | loop, mount, Btrfs metadata | `RealBtrfsBackupTests.cpp` |
| Browse session | Mounted encrypted target, isolated transient manager, real polkit, and unprivileged D-Bus caller | Public manager `OpenBrowseSession` through the compiled client | 0 | Read-only browse mount has `ro,nodev,nosuid,noexec,nosymfollow`, exposes repository data, and leaves the target mounted | Client disconnect removes the session mount; stop the owned manager, remove the rule/user/probe, then common teardown | mount namespaces, D-Bus, polkit, LUKS | `RealBtrfsBackupTests.cpp` and `BrowseSessionClient.cpp` |
| Restore | Committed repository snapshot with real UUID metadata and a catalog | Raw Btrfs send/receive drill, then public restore plan/execute/drill CLI | 0 for supported operations | Plan is non-mutating; raw and public execute preserve content; public drill publishes no destination and leaves no staging | Delete restored subvolumes, stream and catalog fixtures, then common teardown | loop, mount, Btrfs send/receive | `RealBtrfsBackupTests.cpp` |
| Target identity rejection | Temporarily replace the active target UUID in the profile | Public runner execute | non-zero identity error | Error reports Btrfs UUID mismatch; no new local or remote snapshot | Restore the original profile before common teardown | loop, mount, blkid | `RealBtrfsBackupTests.cpp` |
| Cancellation | Trusted copied CLI hook requests cancellation for its own active run | Public runner execute plus public runner cancel from the hook | 1 | JSON is incomplete/cancelled; terminal status is `backup.cancelled`; handled request is absent; no success is recorded | Restore the profile, remove the owned trusted hook and request, then common teardown | loop, mount, process cancellation | `RealBtrfsBackupTests.cpp` |
| Base package boundary | Build and install the Arch base package in a clean container | Package manager install and all three public binaries' `--help` | 0 | Exactly one base package; no KDE/Qt dependency or linked runtime; CLI, runner and manager are executable | Container removal | package manager, dynamic loader | `RealPackageTest.cpp` |
| Configuration installation | Real mounted source/target and their inspected LUKS/Btrfs identities | Public profile wizard/render/install and installation validation CLI | 0 | Active schema-v4 profile contains inspected identities and installed paths; validation accepts it | Remove container configuration with container | loop, mount, LUKS, Btrfs | `RealInstalledRuntimeTest.cpp` |
| Managed target activation | Configured encrypted target is unmounted and closed | Native mount unit and target activation service | 0 | Mapper is activated, target is mounted, and mapper ownership record exists without fstab/crypttab | Stop owned units; common teardown | systemd, udev, device-mapper, mount | `RealInstalledRuntimeTest.cpp` |
| Backup through system D-Bus | Installed policies, real manager/polkit, unprivileged caller and mounted target | Public `StartBackup` and `GetStatus` D-Bus methods | accepted/succeeded | Operation id names the unit; history and reconstructed terminal status succeed; full backup artifacts match | Stop manager/polkit, remove rule/user, common teardown | D-Bus, polkit, systemd, loop, Btrfs | shell, required before stage 13 |
| Runtime preflight and mount CLI | Correct active mounted target | Installed runtime validate and public target mount CLI | 0 | Runtime and CLI both accept the mounted target identity | No scenario-specific artifacts; common teardown | mount, blkid, device-mapper | `RealInstalledRuntimeTest.cpp` |
| Source-on-target rejection | Profile source and local snapshot directory temporarily point inside target Btrfs | Public runner execute | non-zero validation error | Error identifies source/target filesystem aliasing; no repository mutation | Restore profile; delete owned bad subvolume/directory | loop, mount, Btrfs | `RealBtrfsSafetyScenarios.cpp` |
| Incoming symlink escape rejection | `.incoming/home` symlinks to an outside directory with a sentinel | Public runner execute | non-zero safe-path error | Outside sentinel is unchanged; no traversal or backup commit occurs | Remove only the owned symlink/outside directory and restore the incoming directory | loop, mount, Btrfs, symlink-safe filesystem API | `RealBtrfsSafetyScenarios.cpp` |
| Missing incremental parent | Remote snapshots exist while profile points at an empty local snapshot directory | Public runner execute | non-zero parent-selection error | Runtime refuses a non-lineage incremental backup; repository remains unchanged | Restore profile and remove owned temporary local snapshots/directory | loop, mount, Btrfs UUID lineage | `RealBtrfsSafetyScenarios.cpp` |
| Manager independence | Blocking trusted hook holds an active runner | Stop system manager, release hook, wait for runner | 0 | Runner survives manager stop, finishes hook/backup, persists successful history and matching snapshots | Restore profile; remove hook/FIFO/markers; common teardown | systemd, D-Bus, FIFO, Btrfs | shell, required before stage 13 |
| Trusted hook policy | Root-owned executable hook, then wrong owner/mode/parent and symlink variants | Installed backup runtime | 0 for trusted; non-zero for every unsafe variant | Trusted marker appears only for valid hook; precise validation errors for unsafe ownership, mode, parent and symlink | Restore permissions/profile and remove hook fixtures | filesystem ownership, `openat`, Btrfs | `RealTrustedHookTest.cpp` |
| Installed systemd security | Installed backup service unit | Authoritative CMake systemd security contract | 0 | Exposure report exists and remains within accepted policy | Remove report with container | systemd-analyze | `RealPackageTest.cpp` plus `systemd-*-security-tests` |
| Plain mapper lifecycle | Mounted owned encrypted target | Plain unmount/close, then public fixture reopen/remount | 0 | Mapper disappears after close and reappears on the same owned loop; target remounts safely | Common teardown verifies mapper/loop ownership | device-mapper, mount, udev | `RealMapperLifecycleTests.cpp` |
| Sandboxed systemd backup | Move target behind a disposable mount unit; mutate source | Installed hardened profile service | 0 | Required sandbox properties are active; backup succeeds with history, matching snapshots and empty incoming | Stop/remove disposable unit and common teardown | systemd namespaces, mount, Btrfs | shell, required before stage 13 |
| Automatic eject | Enable `autoEject` after a sandboxed service backup | Installed profile service and eject service | 0 | Eject completion advances; target mount and mapper disappear | Restore profile/unit state; common teardown handles already released target | systemd namespaces, mount, device-mapper | shell, required before stage 13 |
| Whole-device provisioning | Empty owned loop device | Public manager provisioning API through compiled client | 0 | GPT, LUKS2 and Btrfs created; profile published only after success | RAII fixture closes the manager and detaches only its verified loop | loop, partition scan, device-mapper, udev | C++, stage 12 |
| Existing-partition provisioning | GPT with selected and sibling partitions containing identity bytes | Public manager provisioning API through compiled client | 0 | Selected partition provisioned; sibling bytes and table identity preserved | RAII fixture closes the manager, unmounts the sibling and detaches only its verified loop | loop, partition scan, device-mapper, udev | C++, stage 12 |
| Unallocated-space provisioning | GPT with one existing partition and a bounded free extent | Public manager provisioning API through compiled client | 0 | New partition remains inside the requested extent; existing partition bytes preserved | RAII fixture closes the manager and detaches only its verified loop | loop, partition scan, device-mapper, udev | C++, stage 12 |
| Existing-target adoption | Compatible encrypted Btrfs repository on an owned loop | Public manager adoption API through compiled client | 0 | No block-device mutation; inspected identities retained; profile published | RAII fixture closes its mapper and detaches only its verified loop | loop, device-mapper, LUKS, Btrfs | C++, stage 12 |
| Provisioning process interruption and recovery | Owned disposable QEMU disks plus transaction-store adapter tests | Public manager API; manager/helper kill, unplug and power-loss restart | interrupted or resumed according to boundary | Durable terminal state, held/released reservation and mapper cleanup are verified | QEMU guest and unit fixtures release their owned resources | systemd, QEMU, block hotplug, device-mapper | `SystemDeviceProvisioningBackendTests.cpp` and `run-hotplug.sh` |
| Fine-grained provisioning phase boundaries | Six boundaries from pre-write through post-profile publication | QEMU fault orchestration | pending (P2) | Device, profile, reservation and transaction must match the documented recovery action after every restart | QEMU guest releases only its disposable disks and recorded mappings | systemd, QEMU, block devices, device-mapper | stage 14 per provisioning plan 4.0 |

All test roots are disposable and uniquely named. C++ fixtures must report
cleanup errors from explicit `close()`; destructors are best-effort `noexcept`
fallbacks only.
